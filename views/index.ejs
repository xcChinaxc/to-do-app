<!-- index.ejs -->
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>List it: a to do app</title>
    </head>
    <body>
        <h1>List it: a to do application</h1>
        
        <form action="/taskItem" method="POST">
            <input type="text" placeholder="add to list" name="taskItem">
            <button type="submit">submit</button>
        </form>
        <h1>Current Tasks</h1>
        <ul class="tasks">
        <% for(let i=0; i < tasks.length; i++) { %>
            <li class="task">
                <span> <%= tasks[i].taskItem %> </span>
                <button id="update-bttn"> Edit Task </button>
            </li>
            <% } %>
        </ul>
        <script src="/main.js"></script>
    </body>
</html>